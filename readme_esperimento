per lanciare un esperimento usare il comando:
python camstyle/main.py -d market --logs-dir logs/market-ide --evaluate --resume camstyle/logs/market-ide/checkpoint.pth.tar

assicurarsi che il path del checkpoint sia corretto e che le immagini di test si trovino all'interno della cartella market/bounding_box_test
